{
  "info": {
    "_postman_id": "f72f591a-d487-48c5-b2ef-468db714f595",
    "name": "Valor Phase1 Local - retained MySQL verification",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "description": "Local development only. Import the empty-secret environment and read postman/README.md. Run the entire collection in order. Creates retained test records; no physical deletion, direct SQL or OTP/provider calls. Negative probes intentionally expect 400/401/403/404/409. Real-MySQL verification requires an operator-confirmed MySQL-backed backend. Scripts never print secrets."
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "(function () {",
          "  function stop() {pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();}",
          "  if (!/^(http:\\/\\/localhost:8081|http:\\/\\/127\\.0\\.0\\.1:8081)\\/?$/.test(String(pm.environment.get('baseUrl')||''))) {",
          "    pm.test('Only the local development URL is allowed',()=>pm.expect(false).to.equal(true));stop();return;",
          "  }",
          "  pm.variables.set('baseUrl',String(pm.environment.get('baseUrl')).replace(/\\/$/,''));",
          "  if(pm.info.requestName==='01 - Health and initialize unique run') {",
          "    [\"customerEmail\", \"customerPhone\", \"technicianEmail\", \"adminAccessToken\", \"adminRefreshToken\", \"customerAccessToken\", \"customerRefreshToken\", \"technicianAccessToken\", \"technicianRefreshToken\", \"customerProfileId\", \"technicianUserId\", \"technicianProfileId\", \"buildingId\", \"liftId\", \"amcId\", \"serviceRequestId\", \"assignmentId\", \"notificationId\", \"runId\", \"customerUserId\", \"adminUserId\", \"employeeId\", \"amcNumber\", \"amcStartDate\", \"amcEndDate\", \"reportId\", \"historyCount\", \"previousAssignmentId\", \"notificationReadAt\", \"branchBuildingId\", \"branchLiftId\", \"branchRequestId\", \"branchAssignmentId\", \"branchHistoryCount\", \"branchReportId\", \"otherCustomerEmail\", \"otherCustomerPhone\", \"otherCustomerUserId\", \"otherCustomerProfileId\", \"otherCustomerAccessToken\", \"otherCustomerRefreshToken\", \"previousAdminRefreshToken\", \"previousCustomerRefreshToken\", \"previousTechnicianRefreshToken\", \"previousOtherCustomerRefreshToken\", \"runReady\", \"runBlocked\"].forEach(k=>pm.environment.set(k, ''));",
          "    const guid=pm.variables.replaceIn('{{$guid}}').replace(/-/g,'');",
          "    const run=Date.now().toString(36)+'-'+guid.slice(0,16);",
          "    const digits=parseInt(guid.slice(0,12),16)%10000000;",
          "    const start=new Date();start.setUTCDate(start.getUTCDate()-2);",
          "    const end=new Date();end.setUTCFullYear(end.getUTCFullYear()+1);",
          "    const generated={runId:run,customerEmail:'customer.'+run+'@example.test',customerPhone:'+1555'+String(digits).padStart(7,'0'),",
          "      technicianEmail:'technician.'+run+'@example.test',employeeId:'PM-'+run,otherCustomerEmail:'other.'+run+'@example.test',",
          "      otherCustomerPhone:'+1555'+String((digits+1)%10000000).padStart(7,'0'),amcNumber:'PM-'+run,",
          "      amcStartDate:start.toISOString().slice(0,10),amcEndDate:end.toISOString().slice(0,10),runReady:'true',runBlocked:'false'};",
          "    Object.keys(generated).forEach(k=>pm.environment.set(k,generated[k]));",
          "  }",
          "  if(pm.environment.get('runReady')!=='true'||pm.environment.get('runBlocked')==='true') {",
          "    pm.test('Start with Health and run the entire collection in order',()=>pm.expect(false).to.equal(true));stop();",
          "  }",
          "})();"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "(function () {",
          "  let passed=true;",
          "  function test(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok)passed=false;}",
          "  let response;try{response=pm.response.json();}catch(_){response=null;}",
          "  const expected=JSON.parse(pm.variables.get('_expected'));",
          "  test('Expected HTTP status',()=>expected.includes(pm.response.code));",
          "  test('Response is an ApiResponse object',()=>response!==null&&typeof response==='object'&&!Array.isArray(response));",
          "  test('ApiResponse status matches HTTP',()=>response&&response.status===pm.response.code);",
          "  test('ApiResponse success matches HTTP outcome',()=>response&&response.success===(pm.response.code>=200&&pm.response.code<300));",
          "  test('Envelope contains message timestamp and data',()=>response&&typeof response.message==='string'&&typeof response.timestamp==='string'&&Object.prototype.hasOwnProperty.call(response,'data'));",
          "  test('Returned history contains only real transitions',()=>!response||!response.data||!Array.isArray(response.data.history)||response.data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
          "  const forbidden=new Set(['password','passwordhash','otphash','tokenhash','stacktrace']);",
          "  function safe(n){return !n||typeof n!=='object'||Object.keys(n).every(k=>!forbidden.has(k.toLowerCase().replace(/_/g,''))&&safe(n[k]));}",
          "  test('No password or credential-hash fields at any depth',()=>safe(response));",
          "  const text=pm.response.text();",
          "  test('No configured password values echoed',()=>['superAdminPassword','customerPassword','technicianPassword'].every(k=>!pm.environment.get(k)||!text.includes(String(pm.environment.get(k)))));",
          "  test('No internal exception details',()=>!/org\\.hibernate|java\\.lang\\.|com\\.valor\\./.test(text));",
          "  if(pm.variables.get('_authResponse')!=='true') {",
          "    function noTokens(n){return !n||typeof n!=='object'||Object.keys(n).every(k=>!['accessToken','refreshToken'].includes(k)&&noTokens(n[k]));}",
          "    test('Non-authentication response contains no tokens',()=>noTokens(response));",
          "  }",
          "  pm.variables.set('_responsePassed',passed?'true':'false');",
          "  if(!passed){pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}",
          "})();"
        ]
      }
    }
  ],
  "item": [
    {
      "id": "882960df-80d7-4c9d-8d57-7bda9184d23c",
      "name": "01 - Health and initialize unique run",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/health",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"superAdminEmail\", \"superAdminPassword\", \"customerPassword\", \"technicianPassword\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Generic healthy response',()=>data.status==='UP');",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "fa423820-6ee8-4de2-b07b-64dc85bcb0ff",
      "name": "02 - SUPER_ADMIN login",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/login/admin",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{superAdminEmail}}\",\n  \"password\": \"{{superAdminPassword}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"superAdminEmail\", \"superAdminPassword\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{superAdminEmail}}\",",
              "  \"password\": \"{{superAdminPassword}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveSession('admin','SUPER_ADMIN');saveId('adminUserId',data.userId);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "f3193529-8ee8-4f3b-9105-a547d72c2cde",
      "name": "03 - SUPER_ADMIN current identity",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('SUPER_ADMIN identity matches session',()=>same(data.userId,'adminUserId')&&data.role==='SUPER_ADMIN'&&data.customerProfile===null&&data.technicianProfile===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "9eeb583e-4f22-4b1d-922a-1d015e271762",
      "name": "04 - Provision unique TECHNICIAN",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/admin/users",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{technicianEmail}}\",\n  \"password\": \"{{technicianPassword}}\",\n  \"role\": \"TECHNICIAN\",\n  \"employeeId\": \"{{employeeId}}\",\n  \"assignedArea\": \"Local verification area\",\n  \"specialization\": \"Local maintenance verification\",\n  \"availabilityStatus\": \"AVAILABLE\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianEmail\", \"technicianPassword\", \"employeeId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{technicianEmail}}\",",
              "  \"password\": \"{{technicianPassword}}\",",
              "  \"role\": \"TECHNICIAN\",",
              "  \"employeeId\": \"{{employeeId}}\",",
              "  \"assignedArea\": \"Local verification area\",",
              "  \"specialization\": \"Local maintenance verification\",",
              "  \"availabilityStatus\": \"AVAILABLE\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Active technician staff response',()=>data.role==='TECHNICIAN'&&data.active===true&&data.email===env('technicianEmail'));saveId('technicianUserId',data.userId);saveId('technicianProfileId',data.technicianProfileId);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "3d281071-9169-4857-b931-d324de69ba2e",
      "name": "05 - TECHNICIAN login",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/login/technician",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{technicianEmail}}\",\n  \"password\": \"{{technicianPassword}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianEmail\", \"technicianPassword\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{technicianEmail}}\",",
              "  \"password\": \"{{technicianPassword}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveSession('technician','TECHNICIAN');check('Technician user and profile ownership',()=>same(data.userId,'technicianUserId')&&same(data.technicianProfile.id,'technicianProfileId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "e97a50fa-0800-43ba-9c32-e10aebdad1b0",
      "name": "06 - Register unique CUSTOMER without role",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/register",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{customerEmail}}\",\n  \"phone\": \"{{customerPhone}}\",\n  \"password\": \"{{customerPassword}}\",\n  \"fullName\": \"Local verification customer\",\n  \"companyName\": \"Local verification\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerEmail\", \"customerPhone\", \"customerPassword\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{customerEmail}}\",",
              "  \"phone\": \"{{customerPhone}}\",",
              "  \"password\": \"{{customerPassword}}\",",
              "  \"fullName\": \"Local verification customer\",",
              "  \"companyName\": \"Local verification\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveSession('customer','CUSTOMER');saveId('customerUserId',data.userId);saveId('customerProfileId',data.customerProfile.id);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "c5da7458-aee0-4125-9aab-486a5ac1cc53",
      "name": "07 - CUSTOMER current identity",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Customer identity and profile match',()=>data.role==='CUSTOMER'&&same(data.userId,'customerUserId')&&same(data.customerProfile.id,'customerProfileId')&&data.technicianProfile===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ebd6ddee-0ec7-4156-9dfd-3ad7a669283f",
      "name": "08 - Register second CUSTOMER for ownership probes",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/register",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{otherCustomerEmail}}\",\n  \"phone\": \"{{otherCustomerPhone}}\",\n  \"password\": \"{{customerPassword}}\",\n  \"fullName\": \"Other local verification customer\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerEmail\", \"otherCustomerPhone\", \"customerPassword\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{otherCustomerEmail}}\",",
              "  \"phone\": \"{{otherCustomerPhone}}\",",
              "  \"password\": \"{{customerPassword}}\",",
              "  \"fullName\": \"Other local verification customer\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveSession('otherCustomer','CUSTOMER');saveId('otherCustomerUserId',data.userId);saveId('otherCustomerProfileId',data.customerProfile.id);check('Customers are distinct',()=>!same(data.userId,'customerUserId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "c549ab7d-8b90-4a72-8eb1-26339816260e",
      "name": "09 - Unauthenticated current-user denial",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 401. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[401]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ef6bfb32-f508-4948-8f0c-3fdb67e1a412",
      "name": "10 - customer cannot provision staff",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/admin/users",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 403. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{technicianEmail}}\",\n  \"password\": \"{{technicianPassword}}\",\n  \"role\": \"TECHNICIAN\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianEmail\", \"technicianPassword\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{technicianEmail}}\",",
              "  \"password\": \"{{technicianPassword}}\",",
              "  \"role\": \"TECHNICIAN\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "89178a43-3de3-456f-b6b6-d6dab85478ad",
      "name": "11 - technician cannot provision staff",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/admin/users",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 403. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"email\": \"{{technicianEmail}}\",\n  \"password\": \"{{technicianPassword}}\",\n  \"role\": \"TECHNICIAN\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianEmail\", \"technicianPassword\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"email\": \"{{technicianEmail}}\",",
              "  \"password\": \"{{technicianPassword}}\",",
              "  \"role\": \"TECHNICIAN\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "9bce3a56-4c19-411e-969e-c4c076e77bca",
      "name": "12 - Create customer building as SUPER_ADMIN",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/buildings",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"customerProfileId\": \"{{customerProfileId}}\",\n  \"buildingName\": \"Verification {{runId}}\",\n  \"address\": \"Local test address\",\n  \"city\": \"Local\",\n  \"status\": \"ACTIVE\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerProfileId\", \"runId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"customerProfileId\": \"{{customerProfileId}}\",",
              "  \"buildingName\": \"Verification {{runId}}\",",
              "  \"address\": \"Local test address\",",
              "  \"city\": \"Local\",",
              "  \"status\": \"ACTIVE\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('buildingId',data.id);check('Building owner and active state',()=>same(data.customerProfileId,'customerProfileId')&&data.isActive===true&&data.activeLiftCount===0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "5514e494-d9a2-42ec-b040-1d84766448c2",
      "name": "13 - Create building-owned lift",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/lifts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"buildingId\": \"{{buildingId}}\",\n  \"name\": \"Local verification lift\",\n  \"serialNumber\": \"{{runId}}\",\n  \"currentStatus\": \"ACTIVE\",\n  \"healthScore\": 100\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"buildingId\", \"runId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"buildingId\": \"{{buildingId}}\",",
              "  \"name\": \"Local verification lift\",",
              "  \"serialNumber\": \"{{runId}}\",",
              "  \"currentStatus\": \"ACTIVE\",",
              "  \"healthScore\": 100",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('liftId',data.id);check('Lift belongs only to intended building',()=>same(data.buildingId,'buildingId')&&data.isActive===true&&data.healthScore===100&&!Object.prototype.hasOwnProperty.call(data,'customerId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ab4fe968-cd19-4d96-8db8-705c9cd29090",
      "name": "14 - Create AMC contract",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/amc-contracts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"liftId\": \"{{liftId}}\",\n  \"amcNumber\": \"{{amcNumber}}\",\n  \"plan\": \"Local verification\",\n  \"startDate\": \"{{amcStartDate}}\",\n  \"endDate\": \"{{amcEndDate}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"liftId\", \"amcNumber\", \"amcStartDate\", \"amcEndDate\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"liftId\": \"{{liftId}}\",",
              "  \"amcNumber\": \"{{amcNumber}}\",",
              "  \"plan\": \"Local verification\",",
              "  \"startDate\": \"{{amcStartDate}}\",",
              "  \"endDate\": \"{{amcEndDate}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('amcId',data.id);check('AMC lift and coverage',()=>same(data.liftId,'liftId')&&data.status==='ACTIVE'&&data.covered===true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "7df8e013-359d-43fc-bbf9-28d5cad28f40",
      "name": "15 - Customer owned buildings and derived count",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/customers/me/buildings",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Owned building and lift count',()=>Array.isArray(data)&&data.length===1&&same(data[0].id,'buildingId')&&same(data[0].customerProfileId,'customerProfileId')&&data[0].activeLiftCount===1);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "19aa8657-d666-4bf8-868d-2f2fb2a5a664",
      "name": "16 - Customer owned lifts and derived coverage",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/customers/me/lifts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Owned lift and derived AMC coverage',()=>Array.isArray(data)&&data.length===1&&same(data[0].id,'liftId')&&same(data[0].buildingId,'buildingId')&&data[0].amcCoverage==='ACTIVE');",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "72449288-7667-4dba-b99d-6e1a5416e2b1",
      "name": "17 - Customer owner-scoped AMC list",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/amc-contracts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Only owned AMC returned',()=>Array.isArray(data)&&data.length===1&&same(data[0].id,'amcId')&&same(data[0].liftId,'liftId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "151313db-605c-4eb1-b3c0-62c045261c29",
      "name": "18 - Other customer cannot see primary buildings",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/customers/me/buildings",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Other customer sees no primary assets',()=>Array.isArray(data)&&data.length===0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "a3b78f61-bc15-4fdc-a9c9-f953e687ade2",
      "name": "19 - Other customer cannot see primary lifts",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/customers/me/lifts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Other customer sees no primary assets',()=>Array.isArray(data)&&data.length===0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "328408e4-7fba-465f-895c-eb103ad9dc26",
      "name": "20 - Other customer cannot see primary amc-contracts",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/amc-contracts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Other customer sees no primary assets',()=>Array.isArray(data)&&data.length===0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "62f8b04f-960b-486c-9efc-cecd9ab9fb1d",
      "name": "21 - CUSTOMER blocked from admin buildings",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/buildings",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 403. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "2a735781-c43f-4b38-ae75-c10c8ee3eeb2",
      "name": "22 - Create CUSTOMER request with JWT ownership",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"liftId\": \"{{liftId}}\",\n  \"title\": \"Local verification request\",\n  \"description\": \"Local retained test record\",\n  \"priority\": \"MEDIUM\",\n  \"serviceType\": \"BREAKDOWN\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"liftId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"liftId\": \"{{liftId}}\",",
              "  \"title\": \"Local verification request\",",
              "  \"description\": \"Local retained test record\",",
              "  \"priority\": \"MEDIUM\",",
              "  \"serviceType\": \"BREAKDOWN\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('serviceRequestId',data.request.id);pm.environment.set('historyCount','0');detail('PENDING',false,1,false);check('Initial event and absent report',()=>data.history[0].fromStatus===null&&data.history[0].toStatus==='PENDING'&&data.report===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "5378b715-5541-4552-b1a5-fc2a421ef6f0",
      "name": "23 - Reject customer-supplied ownership and admin notes",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"liftId\": \"{{liftId}}\",\n  \"title\": \"Local verification request\",\n  \"description\": \"Local retained test record\",\n  \"priority\": \"MEDIUM\",\n  \"serviceType\": \"BREAKDOWN\",\n  \"customerProfileId\": \"{{customerProfileId}}\",\n  \"internalAdminNotes\": \"Must be rejected\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"liftId\", \"customerProfileId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"liftId\": \"{{liftId}}\",",
              "  \"title\": \"Local verification request\",",
              "  \"description\": \"Local retained test record\",",
              "  \"priority\": \"MEDIUM\",",
              "  \"serviceType\": \"BREAKDOWN\",",
              "  \"customerProfileId\": \"{{customerProfileId}}\",",
              "  \"internalAdminNotes\": \"Must be rejected\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "f768a0ed-5a18-4922-9bb0-969f0c6036ec",
      "name": "24 - Other CUSTOMER cannot read primary request",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP [403, 404]. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403, 404]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "7e340c41-0552-46db-8646-cd824c10432d",
      "name": "25 - Unassigned TECHNICIAN cannot read job",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/technician/me/jobs/{{serviceRequestId}}",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP [403, 404]. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403, 404]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "89bf77bf-5eac-4954-a27f-7b5cd2f6ed81",
      "name": "26 - CUSTOMER cannot assign technician",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/assignments",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 403. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"technicianProfileId\": \"{{technicianProfileId}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"technicianProfileId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"technicianProfileId\": \"{{technicianProfileId}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "62ed3f7b-80f3-4967-a293-5dc4c5a7ac8f",
      "name": "27 - SUPER_ADMIN assigns technician",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/assignments",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"technicianProfileId\": \"{{technicianProfileId}}\",\n  \"notes\": \"Local assignment\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"technicianProfileId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"technicianProfileId\": \"{{technicianProfileId}}\",",
              "  \"notes\": \"Local assignment\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('assignmentId',data.activeAssignment.id);detail('ASSIGNED',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "1e2a549f-264b-4071-890e-eb7d4e63dc95",
      "name": "28 - Reassign same technician; replace active row",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/assignments",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"technicianProfileId\": \"{{technicianProfileId}}\",\n  \"notes\": \"Verify retained reassignment\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"assignmentId\", \"serviceRequestId\", \"technicianProfileId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "pm.environment.set('previousAssignmentId',pm.environment.get('assignmentId'));",
              "  const template={",
              "  \"technicianProfileId\": \"{{technicianProfileId}}\",",
              "  \"notes\": \"Verify retained reassignment\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('New active assignment replaces old one',()=>!same(data.activeAssignment.id,'previousAssignmentId'));saveId('assignmentId',data.activeAssignment.id);detail('ASSIGNED',true,0,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "7b733c12-251f-4cfd-a741-7d44b4497385",
      "name": "29 - Old assignment cannot be accepted",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/assignments/{{previousAssignmentId}}/accept",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP [403, 404]. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403, 404]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"previousAssignmentId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "752d975c-f5f7-4468-ad7f-8f300b17b759",
      "name": "30 - Exactly one active job in technician inbox",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/technician/me/jobs?page=0&size=100",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Exactly one active job for this technician',()=>data.totalElements===1&&data.items.length===1&&same(data.items[0].id,'serviceRequestId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "e1365337-4781-4eef-9fe5-2bc9ae364e0a",
      "name": "31 - TECHNICIAN accepts authoritative assignment",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/assignments/{{assignmentId}}/accept",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"assignmentId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('ACCEPTED',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "780f8621-7e02-41a6-8253-82e9f5dfb2ef",
      "name": "32 - Transition to ON_THE_WAY",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"ON_THE_WAY\",\n  \"notes\": \"Local verification ON_THE_WAY\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"ON_THE_WAY\",",
              "  \"notes\": \"Local verification ON_THE_WAY\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('ON_THE_WAY',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "d639a5f3-8b86-4115-9536-1b1800de4739",
      "name": "33 - Transition to REACHED_SITE",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"REACHED_SITE\",\n  \"notes\": \"Local verification REACHED_SITE\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"REACHED_SITE\",",
              "  \"notes\": \"Local verification REACHED_SITE\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('REACHED_SITE',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "05c8e16d-4f31-4248-9999-8eb4260ba129",
      "name": "34 - Transition to DIAGNOSIS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"DIAGNOSIS\",\n  \"notes\": \"Local verification DIAGNOSIS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"DIAGNOSIS\",",
              "  \"notes\": \"Local verification DIAGNOSIS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('DIAGNOSIS',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "43197723-3cf0-4363-9ef2-973c04414c5c",
      "name": "35 - Transition to REPAIR_IN_PROGRESS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"REPAIR_IN_PROGRESS\",\n  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"REPAIR_IN_PROGRESS\",",
              "  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('REPAIR_IN_PROGRESS',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "73495673-06a7-4c78-9aea-330b0ce38e60",
      "name": "36 - Transition to WAITING_FOR_PARTS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"WAITING_FOR_PARTS\",\n  \"notes\": \"Local verification WAITING_FOR_PARTS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"WAITING_FOR_PARTS\",",
              "  \"notes\": \"Local verification WAITING_FOR_PARTS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('WAITING_FOR_PARTS',true,1,false);",
              "check('Waiting notes match submitted notes',()=>data.history[data.history.length-1].notes===JSON.parse(pm.request.body.raw).notes);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "8e74279d-abd3-4131-b5d4-a916ebffffb1",
      "name": "37 - Transition to REPAIR_IN_PROGRESS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"REPAIR_IN_PROGRESS\",\n  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"REPAIR_IN_PROGRESS\",",
              "  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('REPAIR_IN_PROGRESS',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "99e4b544-cd16-4483-83da-22418356ba99",
      "name": "38 - Transition to TESTING",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"TESTING\",\n  \"notes\": \"Local verification TESTING\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"TESTING\",",
              "  \"notes\": \"Local verification TESTING\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('TESTING',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "63358edb-4388-4659-8da4-9eeee5c36683",
      "name": "39 - Transition to REPAIR_IN_PROGRESS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"REPAIR_IN_PROGRESS\",\n  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"REPAIR_IN_PROGRESS\",",
              "  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('REPAIR_IN_PROGRESS',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "fbf79612-c6ba-4129-a836-87091a9015b8",
      "name": "40 - Transition to TESTING",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"TESTING\",\n  \"notes\": \"Local verification TESTING\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"TESTING\",",
              "  \"notes\": \"Local verification TESTING\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('TESTING',true,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "b1c1dc1e-845c-41f5-994a-4bf3629a1b0e",
      "name": "41 - technician completion without report rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 409. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"COMPLETED\",\n  \"notes\": \"Must fail without report\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[409]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"COMPLETED\",",
              "  \"notes\": \"Must fail without report\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "6bf12ce8-9459-4855-8e9c-b5594c3efa86",
      "name": "42 - admin completion without report rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 409. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"COMPLETED\",\n  \"notes\": \"Must fail without report\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[409]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"COMPLETED\",",
              "  \"notes\": \"Must fail without report\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "c855c6d9-9f9b-4bf6-89ec-407514fd6106",
      "name": "43 - Failed completion leaves state and history unchanged",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('TESTING',true,0,false);check('No completion or report yet',()=>data.request.completedAt===null&&data.report===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "0830f08a-fbe1-44ce-9955-8f686212c5c2",
      "name": "44 - TECHNICIAN submits required report",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/technician/me/jobs/{{serviceRequestId}}/report",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"diagnosis\": \"Local inspection identified a test fault\",\n  \"workPerformed\": \"Local simulated maintenance documented\",\n  \"testingResult\": \"Local verification test passed\",\n  \"completionNotes\": \"Retained verification record only\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"diagnosis\": \"Local inspection identified a test fault\",",
              "  \"workPerformed\": \"Local simulated maintenance documented\",",
              "  \"testingResult\": \"Local verification test passed\",",
              "  \"completionNotes\": \"Retained verification record only\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('reportId',data.id);check('Report request assignment and reporter match',()=>same(data.serviceRequestId,'serviceRequestId')&&same(data.assignmentId,'assignmentId')&&same(data.reportedByUserId,'technicianUserId')&&data.diagnosis.trim().length>0&&data.workPerformed.trim().length>0&&data.testingResult.trim().length>0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "eef56fa4-4998-4173-a200-a82db2d01af7",
      "name": "45 - Report does not complete request",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('TESTING',true,0,false);check('Report linked without completion',()=>same(data.report.id,'reportId')&&data.request.completedAt===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "9f986b36-619a-438a-a0f4-28c27b967053",
      "name": "46 - Transition to COMPLETED",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"COMPLETED\",\n  \"notes\": \"Local verification COMPLETED\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"historyCount\", \"serviceRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"COMPLETED\",",
              "  \"notes\": \"Local verification COMPLETED\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('COMPLETED',false,1,false);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "1380e5e5-12c5-4ab9-bc1d-720d7228dfe0",
      "name": "47 - Completed request retained with report and one completion event",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('COMPLETED',false,0,false);check('Completion timestamp exists',()=>typeof data.request.completedAt==='string');check('Report remains visible',()=>data.report!==null&&typeof data.report==='object');check('Report ID is preserved',()=>data.report&&same(data.report.id,'reportId'));check('Exactly one completion event',()=>data.history.filter(h=>h.toStatus==='COMPLETED').length===1);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ae238dd4-a178-40e1-914d-280949f4ab69",
      "name": "48 - Completed job removed from active technician list",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/technician/me/jobs?page=0&size=100",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('No active jobs after completion',()=>data.totalElements===0&&data.items.length===0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "45190efd-4192-4f8b-8a12-cba3a42baf98",
      "name": "49 - Terminal status changes rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{serviceRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 409. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"TESTING\",\n  \"notes\": \"Terminal probe\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[409]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"serviceRequestId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"TESTING\",",
              "  \"notes\": \"Terminal probe\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "a344eb6e-05ed-4bc0-806d-15b5572271ac",
      "name": "50 - Create other customer building",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/customers/me/buildings",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"buildingName\": \"Alternate verification\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"buildingName\": \"Alternate verification\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('branchBuildingId',data.id);check('Other customer owns alternate building',()=>same(data.customerProfileId,'otherCustomerProfileId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "acda49de-d9e2-49d0-9940-627d7d5000cf",
      "name": "51 - Create alternate branch lift",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/lifts",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"buildingId\": \"{{branchBuildingId}}\",\n  \"name\": \"Alternate verification lift\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"branchBuildingId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"buildingId\": \"{{branchBuildingId}}\",",
              "  \"name\": \"Alternate verification lift\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('branchLiftId',data.id);check('Alternate building ownership',()=>same(data.buildingId,'branchBuildingId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "cb5f47e3-7e95-46ac-8130-9d29672c4ffc",
      "name": "52 - Create alternate branch customer request",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"liftId\": \"{{branchLiftId}}\",\n  \"title\": \"Local verification request\",\n  \"description\": \"Local retained test record\",\n  \"priority\": \"MEDIUM\",\n  \"serviceType\": \"BREAKDOWN\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"branchLiftId\", \"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"liftId\": \"{{branchLiftId}}\",",
              "  \"title\": \"Local verification request\",",
              "  \"description\": \"Local retained test record\",",
              "  \"priority\": \"MEDIUM\",",
              "  \"serviceType\": \"BREAKDOWN\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('branchRequestId',data.request.id);pm.environment.set('branchHistoryCount','0');detail('PENDING',false,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "f381fd21-4f53-4c9f-8111-4bbd28657ce3",
      "name": "53 - Primary customer cannot read alternate request",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP [403, 404]. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403, 404]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"branchRequestId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "e99d5381-58e7-4f69-b485-1502c3b09729",
      "name": "54 - Assign alternate branch",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/assignments",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"technicianProfileId\": \"{{technicianProfileId}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"branchRequestId\", \"technicianProfileId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"technicianProfileId\": \"{{technicianProfileId}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('branchAssignmentId',data.activeAssignment.id);detail('ASSIGNED',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "75cda71d-f658-45e0-a778-726de1805821",
      "name": "55 - Accept alternate branch",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/assignments/{{branchAssignmentId}}/accept",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"branchRequestId\", \"branchAssignmentId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('ACCEPTED',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "a9eded84-3615-4aa5-804a-7b87b8ba964e",
      "name": "56 - Alternate branch: Transition to ON_THE_WAY",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"ON_THE_WAY\",\n  \"notes\": \"Local verification ON_THE_WAY\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"ON_THE_WAY\",",
              "  \"notes\": \"Local verification ON_THE_WAY\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('ON_THE_WAY',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "18c950e1-ec5d-43ed-ad1d-3055b7c85ba7",
      "name": "57 - Alternate branch: Transition to REACHED_SITE",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"REACHED_SITE\",\n  \"notes\": \"Local verification REACHED_SITE\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"REACHED_SITE\",",
              "  \"notes\": \"Local verification REACHED_SITE\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('REACHED_SITE',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "f7dea9ec-8d23-42c7-88fd-4972e310ddbb",
      "name": "58 - Alternate branch: Transition to DIAGNOSIS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"DIAGNOSIS\",\n  \"notes\": \"Local verification DIAGNOSIS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"DIAGNOSIS\",",
              "  \"notes\": \"Local verification DIAGNOSIS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('DIAGNOSIS',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "789f302b-1400-4a95-8acd-a034aede1a79",
      "name": "59 - Alternate branch: Transition to WAITING_FOR_PARTS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"WAITING_FOR_PARTS\",\n  \"notes\": \"Local verification WAITING_FOR_PARTS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"WAITING_FOR_PARTS\",",
              "  \"notes\": \"Local verification WAITING_FOR_PARTS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('WAITING_FOR_PARTS',true,1,true);",
              "check('Waiting notes match submitted notes',()=>data.history[data.history.length-1].notes===JSON.parse(pm.request.body.raw).notes);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "f08c45d1-3d4f-445c-98ca-0dd02a85575f",
      "name": "60 - Alternate branch: Transition to REPAIR_IN_PROGRESS",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"REPAIR_IN_PROGRESS\",\n  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"REPAIR_IN_PROGRESS\",",
              "  \"notes\": \"Local verification REPAIR_IN_PROGRESS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('REPAIR_IN_PROGRESS',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "0952635e-81bf-49eb-ab26-4816439f1a6b",
      "name": "61 - Alternate branch: Transition to TESTING",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"TESTING\",\n  \"notes\": \"Local verification TESTING\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"TESTING\",",
              "  \"notes\": \"Local verification TESTING\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('TESTING',true,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "41a36bf5-a4bc-4f7d-aa99-d6074b84e364",
      "name": "62 - Submit alternate branch report",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/technician/me/jobs/{{branchRequestId}}/report",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"diagnosis\": \"Local inspection identified a test fault\",\n  \"workPerformed\": \"Local simulated maintenance documented\",\n  \"testingResult\": \"Local verification test passed\",\n  \"completionNotes\": \"Retained verification record only\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"diagnosis\": \"Local inspection identified a test fault\",",
              "  \"workPerformed\": \"Local simulated maintenance documented\",",
              "  \"testingResult\": \"Local verification test passed\",",
              "  \"completionNotes\": \"Retained verification record only\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('branchReportId',data.id);check('Alternate report ownership',()=>same(data.serviceRequestId,'branchRequestId')&&same(data.assignmentId,'branchAssignmentId')&&same(data.reportedByUserId,'technicianUserId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "2e21ec85-3aa7-41ac-b2cb-8dbd5bf32dd0",
      "name": "63 - Alternate branch: Transition to COMPLETED",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/service-requests/{{branchRequestId}}/status",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"toStatus\": \"COMPLETED\",\n  \"notes\": \"Local verification COMPLETED\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianProfileId\", \"branchHistoryCount\", \"branchRequestId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"toStatus\": \"COMPLETED\",",
              "  \"notes\": \"Local verification COMPLETED\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "detail('COMPLETED',false,1,true);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "bdb72d50-878d-41a2-864c-030c4400c30c",
      "name": "64 - Create IN_APP customer notification",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"recipientUserId\": \"{{customerUserId}}\",\n  \"title\": \"Local verification completed\",\n  \"message\": \"Your local verification workflow is complete\",\n  \"channel\": \"IN_APP\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerUserId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"recipientUserId\": \"{{customerUserId}}\",",
              "  \"title\": \"Local verification completed\",",
              "  \"message\": \"Your local verification workflow is complete\",",
              "  \"channel\": \"IN_APP\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "saveId('notificationId',data.id);check('Notification recipient and initial state',()=>same(data.recipientUserId,'customerUserId')&&data.channel==='IN_APP'&&data.status==='PENDING'&&data.sentAt===null&&data.readAt===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "26972a9b-b50c-49de-b32d-d3ddb9b27816",
      "name": "65 - Reject unsupported EMAIL channel",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"recipientUserId\": \"{{customerUserId}}\",\n  \"title\": \"Unsupported channel probe\",\n  \"message\": \"Must not be delivered\",\n  \"channel\": \"EMAIL\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerUserId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"recipientUserId\": \"{{customerUserId}}\",",
              "  \"title\": \"Unsupported channel probe\",",
              "  \"message\": \"Must not be delivered\",",
              "  \"channel\": \"EMAIL\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "eed5fdd8-b952-4f86-9b1c-8a4d75890c40",
      "name": "66 - Reject unsupported SMS channel",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"recipientUserId\": \"{{customerUserId}}\",\n  \"title\": \"Unsupported channel probe\",\n  \"message\": \"Must not be delivered\",\n  \"channel\": \"SMS\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerUserId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"recipientUserId\": \"{{customerUserId}}\",",
              "  \"title\": \"Unsupported channel probe\",",
              "  \"message\": \"Must not be delivered\",",
              "  \"channel\": \"SMS\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "b3e73ab2-0f39-43a8-aa19-42565ade371a",
      "name": "67 - Reject unsupported PUSH channel",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"recipientUserId\": \"{{customerUserId}}\",\n  \"title\": \"Unsupported channel probe\",\n  \"message\": \"Must not be delivered\",\n  \"channel\": \"PUSH\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerUserId\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"recipientUserId\": \"{{customerUserId}}\",",
              "  \"title\": \"Unsupported channel probe\",",
              "  \"message\": \"Must not be delivered\",",
              "  \"channel\": \"PUSH\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "fee98269-21db-4a3f-b8ca-25e2331a5ae8",
      "name": "68 - customer cannot create notifications",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 403. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"recipientUserId\": \"{{customerUserId}}\",\n  \"title\": \"Forbidden\",\n  \"message\": \"Must not be created\",\n  \"channel\": \"IN_APP\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerUserId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"recipientUserId\": \"{{customerUserId}}\",",
              "  \"title\": \"Forbidden\",",
              "  \"message\": \"Must not be created\",",
              "  \"channel\": \"IN_APP\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "0647ee9c-28db-422a-ac36-c77ce0db601e",
      "name": "69 - technician cannot create notifications",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 403. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"recipientUserId\": \"{{customerUserId}}\",\n  \"title\": \"Forbidden\",\n  \"message\": \"Must not be created\",\n  \"channel\": \"IN_APP\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerUserId\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"recipientUserId\": \"{{customerUserId}}\",",
              "  \"title\": \"Forbidden\",",
              "  \"message\": \"Must not be created\",",
              "  \"channel\": \"IN_APP\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ac9b927d-c2b6-4941-acbb-8d5a62ba5ffa",
      "name": "70 - Customer inbox contains only own in-app record",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications?page=0&size=100",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Inbox owner-scoped and exactly one notification created',()=>data.totalElements===1&&data.items.length===1&&same(data.items[0].id,'notificationId')&&data.items.every(n=>same(n.recipientUserId,'customerUserId')&&n.channel==='IN_APP'&&n.sentAt===null));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "54b11149-2bf7-47f3-a0d9-b69d54468cab",
      "name": "71 - Other customer inbox excludes primary notification",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications?page=0&size=100",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Other recipient inbox remains empty',()=>data.totalElements===0&&data.items.length===0);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "537603c6-641c-41d4-9a1f-adef0703fd12",
      "name": "72 - Other customer cannot mark notification read",
      "request": {
        "method": "PUT",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications/{{notificationId}}/read",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP [403, 404]. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[403, 404]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"notificationId\", \"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "c6e8ab72-39a9-412f-a056-2bc8c307f01c",
      "name": "73 - Customer marks notification READ",
      "request": {
        "method": "PUT",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications/{{notificationId}}/read",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"notificationId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('READ state and ownership',()=>same(data.id,'notificationId')&&same(data.recipientUserId,'customerUserId')&&data.status==='READ'&&typeof data.readAt==='string'&&data.sentAt===null);if(good)pm.environment.set('notificationReadAt',data.readAt);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "30c50586-6e21-4243-b478-a3d9e79fee67",
      "name": "74 - Reread retains original readAt",
      "request": {
        "method": "PUT",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/notifications/{{notificationId}}/read",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"notificationId\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Idempotent READ preserves timestamp',()=>same(data.id,'notificationId')&&data.status==='READ'&&data.readAt===env('notificationReadAt'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "c82566fe-c55c-465b-ad2a-d8f4daa84b71",
      "name": "75 - admin refresh rotation",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{adminRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"adminRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "pm.environment.set('previousAdminRefreshToken',pm.environment.get('adminRefreshToken'));",
              "  const template={",
              "  \"refreshToken\": \"{{adminRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotation returns different refresh token',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20&&data.refreshToken!==env('previousAdminRefreshToken'));if(good){pm.environment.set('adminAccessToken',data.accessToken);pm.environment.set('adminRefreshToken',data.refreshToken);}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "29ee4555-7c08-4ba6-b113-a9da24f7c8c2",
      "name": "76 - admin revoked old refresh rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{previousAdminRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"previousAdminRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{previousAdminRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good)pm.environment.set('previousAdminRefreshToken', '');",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "2b779e93-b660-4edd-b0e0-66d5c954d12a",
      "name": "77 - admin rotated access identity",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotated session retains role and user',()=>data.role==='SUPER_ADMIN'&&same(data.userId,'adminUserId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "09a65bb4-63b9-4c81-a872-8590c623af15",
      "name": "78 - customer refresh rotation",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{customerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "pm.environment.set('previousCustomerRefreshToken',pm.environment.get('customerRefreshToken'));",
              "  const template={",
              "  \"refreshToken\": \"{{customerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotation returns different refresh token',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20&&data.refreshToken!==env('previousCustomerRefreshToken'));if(good){pm.environment.set('customerAccessToken',data.accessToken);pm.environment.set('customerRefreshToken',data.refreshToken);}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "45846760-3117-4458-9cde-86f05532b8c4",
      "name": "79 - customer revoked old refresh rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{previousCustomerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"previousCustomerRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{previousCustomerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good)pm.environment.set('previousCustomerRefreshToken', '');",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "1b09c045-a1e7-48cd-bf7b-127340724f8d",
      "name": "80 - customer rotated access identity",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotated session retains role and user',()=>data.role==='CUSTOMER'&&same(data.userId,'customerUserId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "97ab48ed-28d8-45b2-9c85-88f0eb5353f2",
      "name": "81 - technician refresh rotation",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{technicianRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "pm.environment.set('previousTechnicianRefreshToken',pm.environment.get('technicianRefreshToken'));",
              "  const template={",
              "  \"refreshToken\": \"{{technicianRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotation returns different refresh token',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20&&data.refreshToken!==env('previousTechnicianRefreshToken'));if(good){pm.environment.set('technicianAccessToken',data.accessToken);pm.environment.set('technicianRefreshToken',data.refreshToken);}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "3e67abb8-f2e1-4e8f-abb0-eddb21ea557e",
      "name": "82 - technician revoked old refresh rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{previousTechnicianRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"previousTechnicianRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{previousTechnicianRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good)pm.environment.set('previousTechnicianRefreshToken', '');",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "0e37e25f-23b8-49f7-a027-037b5488a920",
      "name": "83 - technician rotated access identity",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotated session retains role and user',()=>data.role==='TECHNICIAN'&&same(data.userId,'technicianUserId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "8d5b42f5-c7aa-4ea9-80eb-b21c051e0da1",
      "name": "84 - otherCustomer refresh rotation",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{otherCustomerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"true\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "pm.environment.set('previousOtherCustomerRefreshToken',pm.environment.get('otherCustomerRefreshToken'));",
              "  const template={",
              "  \"refreshToken\": \"{{otherCustomerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotation returns different refresh token',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20&&data.refreshToken!==env('previousOtherCustomerRefreshToken'));if(good){pm.environment.set('otherCustomerAccessToken',data.accessToken);pm.environment.set('otherCustomerRefreshToken',data.refreshToken);}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "15115d66-eef9-4639-83e6-0f6aa6d561b2",
      "name": "85 - otherCustomer revoked old refresh rejected",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{previousOtherCustomerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"previousOtherCustomerRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{previousOtherCustomerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good)pm.environment.set('previousOtherCustomerRefreshToken', '');",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ce3eb354-1218-4570-b100-84f5330e9222",
      "name": "86 - otherCustomer rotated access identity",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/me",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values."
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Rotated session retains role and user',()=>data.role==='CUSTOMER'&&same(data.userId,'otherCustomerUserId'));",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "52c9ece6-d827-4208-a464-e1b43cf17c7e",
      "name": "87 - admin logout current session",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/logout",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{adminAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{adminRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"adminRefreshToken\", \"adminAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{adminRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Logout has message-only null data',()=>data===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "cc5807c1-242d-4027-b334-eda16c299a15",
      "name": "88 - admin logged-out refresh cannot be reused",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{adminRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"adminRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{adminRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good){pm.environment.set('adminAccessToken', '');pm.environment.set('adminRefreshToken', '');}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "a5b09f86-e671-4d11-858b-1d2c527ff3fa",
      "name": "89 - customer logout current session",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/logout",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{customerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{customerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerRefreshToken\", \"customerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{customerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Logout has message-only null data',()=>data===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "1996f1cf-4ab2-485b-aec2-5c7ea68810d8",
      "name": "90 - customer logged-out refresh cannot be reused",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{customerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"customerRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{customerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good){pm.environment.set('customerAccessToken', '');pm.environment.set('customerRefreshToken', '');}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "ab2fdfa6-f9ae-4901-b41b-1fda6929d55c",
      "name": "91 - technician logout current session",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/logout",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{technicianAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{technicianRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianRefreshToken\", \"technicianAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{technicianRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Logout has message-only null data',()=>data===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "2f0426a8-a557-4b00-b545-7be052ace297",
      "name": "92 - technician logged-out refresh cannot be reused",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{technicianRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"technicianRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{technicianRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good){pm.environment.set('technicianAccessToken', '');pm.environment.set('technicianRefreshToken', '');}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "5609eb04-f631-412e-a01c-94311d7e8c8e",
      "name": "93 - otherCustomer logout current session",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/logout",
        "auth": {
          "type": "bearer",
          "bearer": [
            {
              "key": "token",
              "value": "{{otherCustomerAccessToken}}",
              "type": "string"
            }
          ]
        },
        "description": "Local retained-record verification. Expected HTTP 200. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{otherCustomerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[200]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerRefreshToken\", \"otherCustomerAccessToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{otherCustomerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "check('Logout has message-only null data',()=>data===null);",
              "})();"
            ]
          }
        }
      ],
      "response": []
    },
    {
      "id": "86810fa7-d2fa-48ae-89c5-d4e62381ae28",
      "name": "94 - otherCustomer logged-out refresh cannot be reused",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          },
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/api/v1/auth/refresh",
        "auth": {
          "type": "noauth"
        },
        "description": "Local retained-record verification. Expected HTTP 400. Environment-only secrets; scripts print no values.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"refreshToken\": \"{{otherCustomerRefreshToken}}\"\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function(){",
              "  pm.variables.set('_expected',\"[400]\");pm.variables.set('_authResponse',\"false\");",
              "  if(pm.environment.get('runBlocked')==='true'){pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "  const required=[\"otherCustomerRefreshToken\"];let ready=true;",
              "  required.forEach(k=>{const v=pm.environment.get(k);if(v===undefined||v===null||String(v).length===0)ready=false;else pm.variables.set(k,v);});",
              "  if(!ready){pm.test('Required local variables are populated',()=>pm.expect(false).to.equal(true));pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);pm.execution.skipRequest();return;}",
              "",
              "  const template={",
              "  \"refreshToken\": \"{{otherCustomerRefreshToken}}\"",
              "};",
              "  function expand(v){if(typeof v==='string'){const m=v.match(/^\\{\\{(\\w+)\\}\\}$/);if(m){const x=pm.environment.get(m[1]);return /Id$/.test(m[1])&&!['runId','employeeId'].includes(m[1])?Number(x):String(x);}return v.replace(/\\{\\{(\\w+)\\}\\}/g,(_,k)=>String(pm.environment.get(k)));}if(Array.isArray(v))return v.map(expand);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,expand(x)]));return v;}",
              "  pm.request.body.update(JSON.stringify(expand(template)));",
              "})();"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "(function () {",
              "  if(pm.variables.get('_responsePassed')!=='true')return;",
              "  const data=pm.response.json().data;let good=true;",
              "  function check(label,predicate){let ok=false;try{ok=Boolean(predicate());}catch(_){ok=false;}pm.test(label,()=>pm.expect(ok).to.equal(true));if(!ok){good=false;pm.environment.set('runBlocked','true');pm.execution.setNextRequest(null);}return ok;}",
              "  const env=key=>pm.environment.get(key),same=(value,key)=>String(value)===String(env(key));",
              "  function saveId(key,value){if(check('Required positive safe ID: '+key,()=>Number.isSafeInteger(value)&&value>0)&&good)pm.environment.set(key,String(value));}",
              "  function saveSession(prefix,role){check('Authentication role is correct',()=>data.role===role);check('Both session tokens are present',()=>typeof data.accessToken==='string'&&data.accessToken.length>20&&typeof data.refreshToken==='string'&&data.refreshToken.length>20);if(good){pm.environment.set(prefix+'AccessToken',data.accessToken);pm.environment.set(prefix+'RefreshToken',data.refreshToken);}}",
              "  function detail(status,active,eventDelta,branch){",
              "    const requestKey=branch?'branchRequestId':'serviceRequestId',assignmentKey=branch?'branchAssignmentId':'assignmentId',historyKey=branch?'branchHistoryCount':'historyCount',customerKey=branch?'otherCustomerProfileId':'customerProfileId';",
              "    check('Request ID and customer ownership match',()=>same(data.request.id,requestKey)&&same(data.request.customerProfileId,customerKey));",
              "    check('Request lifecycle status matches',()=>data.request.status===status);",
              "    if(active)check('One authoritative API-visible active assignment',()=>data.activeAssignment&&!Array.isArray(data.activeAssignment)&&same(data.activeAssignment.id,assignmentKey)&&same(data.activeAssignment.serviceRequestId,requestKey)&&same(data.activeAssignment.technicianProfileId,'technicianProfileId')&&['ASSIGNED','ACCEPTED'].includes(data.activeAssignment.status));",
              "    else check('No active assignment',()=>data.activeAssignment===null);",
              "    check('History contains only real transitions',()=>data.history.every(h=>h.fromStatus===null||h.fromStatus!==h.toStatus));",
              "    const before=Number(env(historyKey));",
              "    check('History contains exactly the expected new events',()=>Array.isArray(data.history)&&data.history.length===before+eventDelta&&new Set(data.history.map(h=>h.id)).size===data.history.length);",
              "    if(good)pm.environment.set(historyKey,String(data.history.length));",
              "  }",
              "if(good){pm.environment.set('otherCustomerAccessToken', '');pm.environment.set('otherCustomerRefreshToken', '');}",
              "})();"
            ]
          }
        }
      ],
      "response": []
    }
  ]
}
