# Valor rebuild progress

- Branch: `main`; no branch changes or push. Unrelated README.md changes preserved and excluded.
- Completed: Stage 3 service requests, technician assignments, immutable status history and structured reports; Stage 1 authentication and Stage 2 assets retained. Notifications remain deferred until Stage 4.
- Files: added `workflow` entities/enums/repositories/DTOs/controller/service/error handling, `WorkflowIdentityAccess`, V3, H2 test adapter and workflow integration tests; updated explicit scan packages, route security, technician profile accessors, API contract and Stage-2 migration/entity-count assertions to allow the added stage.
- Commands: H2 syntax probe confirmed STORED is unsupported; `mvn clean test` passed for the existing 38 tests with V3, then final `mvn clean test` and `mvn clean package` passed with the expanded suite.
- Final tests: 55 passed, 0 failures, 0 errors, 0 skipped in each final Maven command. All 38 existing Stage-1/2 tests plus 17 new workflow tests ran; no skip flags.
- Coverage: V3/validate, 12 canonical entities and signed IDs, strict JWT ownership, initial PENDING event, assignment/acceptance boundaries, same-technician reassignment and stale-report invalidation, every allowed/disallowed lifecycle edge, cancellation/parts notes, report ownership/uniqueness/required fields/terminal protection, admin report gates, atomic completion, paging and safe 401/403/404 envelopes. Separate committed H2 connections test concurrent duplicate active assignments; concurrent HTTP completion produces one success and one event.
- Schema: V1 and V2 unchanged. V3 creates exactly service_requests, technician_assignments, service_status_history and service_reports, with restrictive FKs and named VARCHAR checks. The exact MySQL STORED generated active_request_id and unique index are present; no writable generated-field mapping or request/technician pair uniqueness exists.
- Test limitation: H2WorkflowMigrationAdapter exists only on the test classpath and activates only for the H2 driver. It reads the real migration resources, removing only STORED from V3 for H2. V3 test checksums consequently differ from MySQL. It does not prove MySQL stored-column/storage-engine behavior and is not packaged in the application JAR.
- Static review: no unsigned BIGINT, direct request technician FK, cascade removal or plaintext credential literals in the new workflow code/V3. Legacy service entities and unversioned source remain outside explicit scans; runtime tests confirm they are not mapped. SQL initialization remains disabled, effective Hibernate mode validate. The pre-existing earlier production update default remains overridden by its later validate setting; no runtime properties were changed.
- MySQL: V1/V2 were reported verified before this stage. V3 was NOT run against MySQL here. Neither valor_world_dev nor valor_lift_db was accessed or modified; no backend runtime or manual MySQL schema operation was started.
- Blockers: no build/test blockers; real-MySQL V3 verification remains outstanding.
- Next: separately authorize V3 MySQL verification; Stage 4 notifications remain unimplemented. No visits/checklists/parts/attachments, payments, inventory, client code or Valor-technician changes.
